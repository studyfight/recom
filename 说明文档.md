# recom2 使用说明（对接版）

recom2 为"纯接口对接"版本，提供两个核心API：

1、套餐推荐接口 - 基于用户信息推荐体检套餐
2、问答接口 - 基于推荐结果回答用户问题（支持大模型增强）

## 1) 启动与自测

### 启动服务
```bash
conda activate agent_chat
uvicorn recom2.web:app --host 127.0.0.1 --port 8000 --reload
```

健康检查：`GET http://localhost:8000/health` 应返回 `{ "ok": true }`

### 自测（两种）
- 快速自测（curl）
```bash
curl -X POST "http://localhost:8000/api/v1/agents/package-recommendations" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "test_001",
    "age": 40,
    "gender": "male",
    "budget": 2000,
    "purpose": "慢病管理",
    "health_concerns": ["糖尿病"],
    "family_history": ["高血压"],
    "lifestyle_factors": ["吸烟"],
    "top_n": 3
  }'
```

# 问答接口
- 1、完整测试
步骤1：先获取推荐结果
```bash
curl -X POST "http://localhost:8000/api/v1/agents/package-recommendations" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "test_001",
    "age": 40,
    "gender": "male",
    "budget": 2000,
    "purpose": "慢病管理",
    "health_concerns": ["糖尿病"],
    "family_history": ["高血压"],
    "lifestyle_factors": ["吸烟"],
    "top_n": 3
  }'
```
步骤2：问答测试（需要手动把上一步的 data 复制到 context）
```bash
curl -X POST "http://localhost:8000/api/v1/agents/qa" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "test_001",
    "question": "为什么推荐这些套餐？",
    "context": {
      "user_id": "test_001",
      "recommended": [
        {
          "package_name": "糖尿病筛查",
          "score": 1.83,
          "price": 1482.2,
          "recommendation_reason": "价格在预算内..."
        }
      ]
    }
  }'
```
# 具体实例（推荐结果已经录入）
```bash
curl -X POST "http://localhost:8000/api/v1/agents/qa" \
  -H "Content-Type: application/json" \
  -d '{
  "user_id": "test_001",
  "question": "为什么推荐这些套餐？",
  "context": {
    "user_id": "test_001",
    "recommended": [
      {
        "package_name": "糖尿病筛查",
        "package_info": {
          "code": "CHRONIC_DIABETES",
          "name": "糖尿病筛查",
          "price": 1482.2,
          "category": "和系列套餐",
          "key_features": [
            "一般检查",
            "血常规、生化检查",
            "糖尿病筛查",
            "糖化血红蛋白"
          ],
          "tags": [
            "糖尿病筛查",
            "慢性病管理"
          ],
          "health_focus": [
            "糖尿病筛查",
            "慢性病管理"
          ],
          "description": "包含一般检查、血常规、生化检查、糖尿病筛查、糖化血红蛋白；适合糖尿病筛查、慢性病管理"
        },
        "score": 1.8383333333333334,
        "filtered_key_features": [
          "一般检查",
          "血常规、生化检查",
          "糖尿病筛查",
          "糖化血红蛋白"
        ],
        "recommendation_reason": "价格在预算内(¥1482/预算¥2000)；包含一般检查、血常规、生化检查、糖尿病筛查；标签命中：慢性病管理、糖化血红蛋白、糖尿病专项筛查；适合慢病管理"
      },
      {
        "package_name": "臻爱亲情套餐(男士)",
        "package_info": {
          "code": "LOVE_FAMILY_DELUXE_M",
          "name": "臻爱亲情套餐(男士)",
          "price": 1664,
          "category": "爱系列套餐",
          "key_features": [
            "一般检查、五官科、腰臀围",
            "生化26项、甲功三项",
            "同型半胱氨酸、糖化血红蛋白",
            "甲胎蛋白、癌胚抗原、CA19-9、NSE",
            "颈动脉超声、胸部CT"
          ],
          "tags": [
            "糖尿病筛查",
            "慢性病管理",
            "肿瘤筛查",
            "心血管检查",
            "影像学检查"
          ],
          "health_focus": [
            "糖尿病筛查",
            "慢性病管理",
            "肿瘤筛查",
            "心血管检查",
            "影像学检查"
          ],
          "description": "包含一般检查、五官科、腰臀围、生化26项、甲功三项、同型半胱氨酸、糖化血红蛋白、甲胎蛋白、癌胚抗原、CA19-9、NSE；适合糖尿病筛查、慢性病管理",
          "constraints_parsed": {
            "contains_male_specific": true
          },
          "highlights_parsed": [
            "颈动脉超声",
            "骨密度"
          ]
        },
        "score": 1.7083333333333333,
        "filtered_key_features": [
          "一般检查、五官科、腰臀围",
          "生化26项、甲功三项",
          "同型半胱氨酸、糖化血红蛋白",
          "甲胎蛋白、癌胚抗原、CA19-9、NSE",
          "颈动脉超声、胸部CT"
        ],
        "recommendation_reason": "价格在预算内(¥1664/预算¥2000)；包含一般检查、五官科、腰臀围、生化26项、甲功三项、同型半胱氨酸、糖化血红蛋白；标签命中：中老年优先、中老年健康评估、动脉硬化检测；亮点：颈动脉超声、骨密度；适合慢病管理"
      },
      {
        "package_name": "煦暖套餐(男士)",
        "package_info": {
          "code": "WARM_XU_M",
          "name": "煦暖套餐(男士)",
          "price": 1058,
          "category": "暖系列套餐",
          "key_features": [
            "曼暖套餐全部项目",
            "糖化血红蛋白检测",
            "糖类抗原CA-19-9",
            "前列腺特异性抗原检测",
            "甲状腺超声检查",
            "14-C呼气试验"
          ],
          "tags": [
            "糖尿病筛查",
            "慢性病管理",
            "肿瘤筛查",
            "前列腺检查"
          ],
          "health_focus": [
            "糖尿病筛查",
            "慢性病管理",
            "肿瘤筛查",
            "前列腺检查"
          ],
          "description": "包含曼暖套餐全部项目、糖化血红蛋白检测、糖类抗原CA-19-9、前列腺特异性抗原检测；适合糖尿病筛查、慢性病管理",
          "constraints_parsed": {
            "contains_male_specific": true
          }
        },
        "score": 1.6883333333333332,
        "filtered_key_features": [
          "曼暖套餐全部项目",
          "糖化血红蛋白检测",
          "糖类抗原CA-19-9",
          "前列腺特异性抗原检测",
          "甲状腺超声检查",
          "14-C呼气试验"
        ],
        "recommendation_reason": "价格在预算内(¥1058/预算¥2000)；包含曼暖套餐全部项目、糖化血红蛋白检测、糖类抗原CA-19-9；标签命中：全人群通用、前列腺检查、幽门螺杆菌检测；适合慢病管理"
      }
    ]
  }
}'
```
# 测试无context 的友好提示
```bash
curl -X POST "http://localhost:8000/api/v1/agents/qa" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "test_001",
    "question": "为什么推荐这些套餐？"
  }'
```
预期结果：
```json
{
  "trace_id": "qa_xxx",
  "status": "success",
  "timestamp": "...",
  "answer": "您的问题需要基于推荐结果来回答。请先调用套餐推荐接口获取推荐结果，然后将结果作为 context 传入问答接口。\n\n调用流程：\n1. 先调用 /api/v1/agents/package-recommendations 获取推荐\n2. 保存返回的 data 字段\n3. 调用问答接口时将 data 作为 context 参数传入"
}
```
# 测试不需要context的通用问题
```bash
# 询问检查项目（不需要推荐结果）
curl -X POST "http://localhost:8000/api/v1/agents/qa" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "test_001",
    "question": "糖化血红蛋白是什么？"
  }'
# 询问注意事项（不需要推荐结果）
curl -X POST "http://localhost:8000/api/v1/agents/qa" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "test_001",
    "question": "体检前需要注意什么？"
  }'
```

- 脚本自测（并发/结构/排序校验）
  使用 `recom2/test_recom2.py` 脚本（已包含 HTTP 连通性、参数校验、并发安全用例）。
```bash
conda activate agent_chat
python recom2/test_recom2.py
```

说明：脚本会在服务未启动时跳过HTTP用例，仅执行本地直调；启动服务后重跑可完整校验。

## 2) 对接POST接口

- 接口路径：`POST /api/v1/agents/package-recommendations`
- Content-Type：`application/json`

### 请求字段（JSON）
- user_id (string, 必填)：用户唯一标识
- age (int, 选填)：年龄
- gender (string, 选填)："male" | "female"
- budget (number, 选填)：预算
- purpose (string, 选填)：体检目的（如：常规体检/入职体检/慢病管理/肿瘤筛查/婚前体检/高端体检）
- health_concerns (string[])：健康关注点
- family_history (string[])：家族史
- lifestyle_factors (string[])：生活方式
- top_n (int, 选填, 默认3)：返回套餐数量

示例：
```json
{
  "user_id": "u123",
  "age": 40,
  "gender": "male",
  "budget": 2000,
  "purpose": "慢病管理",
  "health_concerns": ["糖尿病", "高血压"],
  "family_history": ["高血压"],
  "lifestyle_factors": ["吸烟"],
  "top_n": 3
}
```

### 响应字段（JSON）
- trace_id (string)：请求追踪ID
- status (string)："success"/"error"
- timestamp (string)：时间戳（ISO8601）
- data (object)：业务数据
  - user_id (string)：回响请求的user_id
  - recommended (array)：推荐列表
    - package_name (string)
    - package_info (object)：包含 name/code/price/tags/key_features 等
    - score (number)
    - filtered_key_features (string[])
    - recommendation_reason (string)

响应示例：
```json
{
  "trace_id": "req_a1b2c3d4",
  "status": "success",
  "timestamp": "2025-01-15T10:00:00+08:00",
  "data": {
    "user_id": "u123",
    "recommended": [
      {
        "package_name": "糖尿病筛查套餐(男士)",
        "package_info": { "name": "糖尿病筛查套餐", "code": "TC_DIABETES_001", "price": 1280, "tags": ["糖尿病筛查"], "key_features": ["糖化血红蛋白","空腹血糖"] },
        "score": 2.48,
        "filtered_key_features": ["糖化血红蛋白","空腹血糖"],
        "recommendation_reason": "价格在预算内；标签命中：糖尿病筛查；适合慢病管理"
      }
    ]
  }
}
```
## 3）对接POST接口-问答接口
- 接口路径：`POST /api/v1/agents/qa`
- Content-Type：`application/json`
### 请求字段（JSON）
- user_id (string, 必填)：用户唯一标识
- question (string, 必填)：用户问题
- gender (string, 选填)：用户性别
- context (object, 选填但强烈推荐)：推荐结果上下文（从推荐接口返回的 data 字段）
- 调用流程：
1. 调用推荐接口 → 获取套餐推荐结果
2. 保存推荐接口返回的 data 字段
3. 用户提问 → 调用问答接口，将 data 作为 context 传入
4. 获得智能回答
5. 用户继续提问（重复步骤3-4）
示例：
```json
// 第1步：调用推荐接口（同上第2章）
// 第2步：调用问答接口
{
  "user_id": "u123",
  "question": "为什么推荐这些套餐？",
  "context": {
    "user_id": "u123",
    "recommended": [
      {
        "package_name": "糖尿病筛查套餐(男士)",
        "score": 2.48,
        "price": 1280,
        "recommendation_reason": "价格在预算内；标签命中：糖尿病筛查",
        "key_features": ["糖化血红蛋白", "空腹血糖"]
      }
    ]
  }
}
```
其他问题示例：
```json
// 询问检查项目
{"user_id": "u123", "question": "糖化血红蛋白是什么？", "context": {...}}

// 套餐对比
{"user_id": "u123", "question": "这几个套餐有什么区别？", "context": {...}}

// 注意事项
{"user_id": "u123", "question": "体检前需要注意什么？", "context": {...}}
```

### 响应字段（JSON）
- trace_id (string)：请求追踪ID
- status (string)："success" / "error"
- timestamp (string)：时间戳（ISO8601）
- answer (string)：回答内容（支持换行符 \n）

响应示例：
```json
{
  "trace_id": "qa_x1y2z3a4",
  "status": "success",
  "timestamp": "2025-01-18T01:00:00+08:00",
  "answer": "推荐糖尿病筛查套餐主要基于以下原因：\n\n【结论】\n首推『糖尿病筛查套餐(男士)』，该套餐综合评分最高(2.48分)，主要因为：\n1. 完全匹配您的健康关注点（糖尿病）\n2. 包含核心检查项目：糖化血红蛋白、空腹血糖、胰岛素等\n3. 价格在预算范围内（¥1280/预算¥2000）\n4. 适合40岁男性的慢病管理需求\n\n【对比】\n与其他推荐套餐相比，该套餐在糖尿病筛查方面更专业..."
}
```
支持的问题类型
1、推荐理由：为什么推荐这些套餐、推荐理由是什么
2、套餐对比：这几个套餐有什么区别、哪个更适合我、应该选哪个
3、项目解释：糖化血红蛋白是什么、CA19-9检查什么、有什么用
4、注意事项：体检前需要注意什么、需要空腹吗、有哪些准备事项

## 4) 套餐推荐目录说明（对接所需）
- `recom2/web.py`：对接API，仅保留POST接口与健康检查（每次请求创建agent，保证并发安全）
- `recom2/recommendation_agent.py`：核心推荐逻辑
- `recom2/packages.json`：套餐库
- `recom2/config/*.json`：规则与标签映射
- （可选）`recom2/test_recom2.py`：一键自测脚本

## 5) 备注
- recom2 不包含UI页面与问答接口；仅提供标准JSON接口
- FastAPI自带 `/docs` 文档页仍可用于联调演示

最后更新：2025-10-15

